<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.github.florent37.assets_audio_player">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <permission android:name="com.github.florent37.assets_audio_player.lib" android:protectionLevel="signature"/>

    <application>
        <receiver android:name=".notification.NotificationActionReceiver"/>
        <receiver android:name=".notification.CustomMediaButtonReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <service
            android:name=".notification.NotificationService"
            android:enabled="true"
            android:exported="false"
            android:permission="com.github.florent37.assets_audio_player.lib">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
            -->
        </service>

    </application>

</manifest>
